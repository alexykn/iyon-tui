# Parent reading notes — 09 projection/smoothing
Fully read 1–410,411–820,821–1230,1231–1638. Source and claims baseline4355c02; no fresh tests. Originals immutable.

## Inventory and integration
Projection subtree6 production files~1133LOC plus~1058 tests: value,validate,projector,compose,smooth,mod. Root private production Rust module; migrated public-shaped tests internal not external surface. Generic coordinates only, no product semantics.
Projection<T>{source_base,stable_through,source_end,sealed,spans}; spans{source,values}, zero-value nonempty elision. Builder finish validates ordered exact contiguous nonempty coverage, bounds, frontier at span edge, sealed=>stable=end, empty envelope=>no spans. Reversed StreamRange panics before validation. map/ref/spans/fallible mapping preserves source segmentation.
Relation samebase,prefixend,stable<=input,seal requires fully sealed input. Transition monotonic base/end/stable; compaction not beyond previousstable; stable overlapping spans retain values/segmentation; sealed only identical successor. Explicit validator not centrally executed runtime.
Projector mutable no Send/Sync, restart identity default, next_wakeup None, advance false. Then owned stages, ordered stage+relation checks, four error variants. Independent source check compose.rs66–105: both advances ALWAYS called same now (no boolean shortcircuit); wake min; restart second then first with debug_assert nonforward. Runtime selected parser dispatch NOT Then chain.
Smooth config default16ms spring2 min20 max800values/sec; validated builder disallows zero/invalid/no progress. Stable spans atomic, weight values.len; free elision, one immediate weightedspan episode; pending queue metadata not values. Credit rate clamp(pendingunits*spring,min,max)*elapsed; absolute caller Instant; first advance/ensure_clock establishes time without rebasing existing; sealed flushidentity; output clones published values; output_from fallback fullprefix if invalid/inside span. append_span_many/project_incremental no observed active callers; queued_through writeonly.
HostContentRegistry/ConnectorExecution owns parser state separately ConnectorDelivery(Smooth,graphemeProjection,indexedgeneration/revision/sealed,candidatefrontier). On Source change full retained grapheme scan/rebuild, preserving crosspage cluster carry. Pureticks advance frontier, reuse semantic/layout/physical/VisibilityIndex. Reveal count uses partition_point; delivery revision invalidates candidate projection not retained semantic/prepared caches.
Active deadlines HashMap and active connectors HashSet; due selection scans D active entries, not all registry and NOT sublinear due-only lookup; nextdeadline min O(D). Hostauthoritativeclock, combined components/content deadline, nativewait cap16ms; TS/native wait asymmetry othernotes.
Caches each2: semantic(source/generations/rev/base/end/seal/funnel/hyperlinks, NOT width/theme/delivery); prepared(semantic,theme,width,finalized/physicalrequirements); projection additionally wrap/delivery etc; prefixproof semantic/stableend/width from sealedprefix. Candidate/committed Arc identity/frontier receipt binding separate from Smooth mutable progress. Recorded failures block same key; retry newer revision; no raw fallback.

## Corrections and issues
- Report4.1 says Projection fields inaccessible outside module BECAUSE pub(crate): incorrect. Independent source value.rs1–35 shows envelope/spans and span fields pub(crate), accessible entire crate. Builder is convention/runtime construction boundary, not module-enforced invisibility to siblings. Carry explicit erratum.
- Report history framing treats L1 onlycorroboration/sourceauthoritative; current source describes actual, but approved PERF13/APIH/L1/PREV5 handoffs normative unless superseded/owneraccepted justifiedimprovement. Correct synthesis.
- TS exported Projection/Builder/Smooth standalone weaker data API (no stable/sealed,envelope, gaps/empty accepted; Smooth through-only), no native pipeline consumers found. Needs APIH intendedcontract + grouped followup; not yet parent sourcechecked.
- Smooth assumes monotonic stable inputs but no transition enforcement, envelope-only pending rebuild; generic stable regression/payload replacement could violate assumptions. Active Source stable=end avoids ordinary regression. Do not label runtime bug without reachable proof.
- append_span_many unused mutates then validates; no rollback, assertion panics. Latent internal helper not active failure.
- Full grapheme rebuild per Source rev and Smooth generic cloning distinct from zero parser/surfaceclone puretick claims. Measure where contract demands.
- Private Rust/ProjectorExt testonly intentional package boundary not inherent API issue.
- Diagram09 shows Then->Smooth dependency inaccurately as structural chain; both implement Projector, Smooth independent.
Useful tests coreconstruction/transition/relation, composition fourerrors/nonSend, Smoothatomic/elision/deadlines; application delayedreceipt/newertick, independentconnectors, tickzero parser/surfaceclones, cleanup. No freshcommands.
