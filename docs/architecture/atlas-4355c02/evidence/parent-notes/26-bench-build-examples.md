# Parent integration notes — 26 benches/build/examples

Fully read1589 lines ranges1–400,401–800,801–1200,1201–1589. Static only. No benchmark/build execution; checked-in data historical, not current baseline results.

## Build/support model
-3member Cargo workspace Rust2024/resolver3; core unpublished native-host/test-util/perf-counters, hidden required-feature tui_perf binary; native cdylib+rlib default NAPI, fast-view-abi, perf-counters, structural direct-ffi qualification. Direct content FFI ALWAYS separate default path; blanket default 'no direct symbols' must scope STRUCTURAL direct qualification.
-stage-native parses features, sorted target directory suffix, release build with default1job, copy stable package native/iyon-tui-native.node, require+removedsurface/buildmarkers/contentartifactidentity+nm checks. loader canonical shared addon path with optional override.5 platform mappings vs CI Linuxx64/macOSarm64 and historical local macOSarm64 only.
-Root just build/release doesn't stage. Package smoke actual plain source+funnel+contentport render+append+screen, not full controls/history/input qualification. Generated artifact ignored; addon buildmarker s6 static not hashproof.
-54 Rust perf counters relaxed global atomics, no synchronization guarantee; operationalzero withoutfeature. testlock+threadlocal enable for tests. TS retainedidentity counters always updated hotroute, phaseinstrument optional; wakebroker11 counters scheduling not layoutcost. RSS coarse not allocator/liveobject proof. Hidden Rust benchmark requires perf-counters; scout's claim ordinary disabled binary emitszeros misleads because Cargo requiredfeature preventsnormaldisabled run.

## Benchmark inventory and meaning
-Rust clone,6workloads×4sizes×4patterns, static1000/live1001History; shared subtree and identity intentional; paintgate3warmups, cached layout+paint, per-iteration epochs. Parent source confirms FRESH and REBUILT_EQUIVALENT match arms IDENTICAL fixtureconstruction+render; not distinct implementation paths. All patterns different constructioncost except these2 identical. begin_epoch(defaulttheme) doesnot itself mean theme revisionchanged/cache cleared.
-TS8 runners/helpers + generated60-case metadata: T15strictretained structuralprepare+commit/phase timings; contentappend/flush; L1plain/Markdown/diff/ANSI (Markdownunbounded retention); sourceappend/snapshot/replace/truncate lifecycle; state paint/noop/geometry/unmounted/positions; integrated3plane trace; textlanes. Requiredcounter modes vs optionaltiming status must distinguish. Harness flush/snapshot boundaries may drive work, not interactive autonomousframesproof.
-T15 semanticconstruction + transport/host total, fixed nativehost80x24/headless, not publicTui pipeline or physicalterminal IO. Samearchitecture cache/route counters supplement timing, do not prove native complexity invisible toinstrumentation. No CI threshold fullmatrix;20sample smoke2warmup only.
-Generic widthprobe realTTY/stty/CPR,590parrot frames bulkfixtures, publicconsumer package. Gates bindingallowlist/declarationclosure/ownership heuristic checks; declarations emits temporary then nameability+testingprobe, not published package test. Old S0/perf package paths and records historical.

## Independent checks/corrections
Read full stage-native.ts, artifact.ts, perf12_t15_authoritative_case.ts, ci.yml and perf_bench.rs291–456.
1.Stage COPY happens BEFORE require/surface/marker/contentidentity/nm qualification. Report9.6 'overwrites only after successful build and validation steps' too broad/wrong if includesqualification. Build/missingfile errors retainoldartifact; POSTCOPY validation failure leavesnew UNQUALIFIED artifact. No rollback observed. Treat as buildlifecycle fact, not demonstrated shippingfailure.
2.Loader selects FIRST EXISTING override/default candidate; nonexistent explicit override FALLSBACK to stagedaddon, existing invalid override fails loading ratherthan nextcandidate. This is artifactselection fallback, not transportfallback; provenance sensitivity.
3.CRITICAL current CI 'direct-ffi' T15 uses SAME nativeViewAbiSession NAPI + RetainedRootBoundary path. T15_TRANSPORT and T15_CANDIDATE env values only printed metadata; changing label NEVER selects directFFI transport. ci.yml labels transport feature_gated_direct_ffi after feature-enabledstage, but script still safeNAPI! Need reconcile42/41: valid addon exportpresence qualification + NAPIonfeaturebuild smoke, NOT structural directFFI performance/dispatch qualification. Route-strict retained guard verifies retaineddomain, not printedtransport. Gitsha/rustc/target/addonhash environment strings defaultunknown; not selfverified. process_isolated:true literal, script doesn't spawnisolation. Bootstrap unseeded Math.random, no numberpositive envvalidations, screen rows observednot expectedoutputassert in T15.
4.RustFRESH=REBUILT_EQUIVALENT exactsource arms, document as sameroute duplicationnot extraarchitecture. Featureoffzero observation applies instrumentation API generally, hiddenbinary requiresfeature.

## Integration pointers
Use26 for supportinventory/provenance matrix;42 benchmarkintegrity must be reconciled with directlabel issue. Do not claim codegen/unexecutedCI gates passed. No source changes/newtests.
