# Parent notes31 — fully read1242
Ranges1–415;416–830;831–1242. Static only.
Immutable structural root replacement supported public authoring. Internal axisSetChildForTransport row/column bounds preserves omittedtrack; axisSplice inserts/removes/rangereplaces, omittedtrack normal; gridSetCell logicalcoordinate preserves spans/alignment. Fresh NodeId + derivation weakmetadata + attachments + wide persistent override. Root retained tryDerivation sends replacement/insertedchildren only; expected stale once or direct semantic materialization same architecture, NOT older completeobject fallback.
Native View Arc immutable map_node shallowclone/newViewId flags; PersistentSeq branch32 leaf/branchArc shares untouched chunks, set pathcopy/splice splitconcat; TS wide threshold1024 lazychildren/rows flatten only when asked. ensure hint/tx/eligibleNodeId/cycle/derivation/materializer/stateattachment/temp lease.
Direct native-view-abi tryRetainedAxisSetChildRender/AxisSpliceRender/GridSetCellRender test/bench callers only, not canonical root/controls. previous View unused; previousRef actual authority. Child materialization -> ABI -> hostRender/install -> temp release; caller owns returnedroot. Standalone allocates splice buffer vs DAG scratch.
Native nested axis/grid replacement generated paths physicallyimplemented but TS production doesn't call. Paths selectors+expectedkinds not ownedViews; fixed wrappers/axis/grid/hanging; batch rebuildparent once. No pathsplice. EditTxn text-layout ONLY strongbase expected<=256 depth<=4 transactionlane, duplicates/path/count/bytes validated; trie commonancestors; stage localrefs -> host.render -> publishrefs; abort drops staging.
No firstclass reorder/reparent/mixedstructuraltransaction/gridrowcellsplice/fixedarityinsert. Reconstruct desired semantic tree can express UI result; unsupported incrementalAPI != unsupported authoring. Keyed parentlocal execution reconciliation != arbitrary reparent or native occurrence movement. Immutable child sharing still constrained attachment uniqueness.
Prepare failure preserves old roots; commit accepts desired not physicalvisibility; composition deferredremoval finalizes after allcommits, pathological throws not rollback. Root desired/visible/superseded leases.
CRITICAL parent correction: read host.rs1091–1120,1320–1323 render=set_desired_view thenflush_pending. Report§5.6 priorhostroot remains on edittransaction host.render failure WRONG generally: desired mayalreadychange before failedflush, visible priorframe can remain; stagedNativeRef publication then skipped. Same§5.4 directhelper failure. Group lifecycle investigation, no pretend completeatomicrollback.
Read compose.ts axis/grid construction/match sections and fullfile search: NO axisSetChildForTransport/axisSpliceForTransport/gridSetCellForTransport invocation. Changed public axis/grid composition REBUILDS parent, wide equality bails/rebuilds. §5.1 “Yes when derivedroot published” conditional materializer capability NOT ordinary public producer route proof. Public index doesn'texport internalhelpers. Merge41route census.
Source normative framing reconcile handoff oracle. Counts/tests sourceonly not census/pass proof.
