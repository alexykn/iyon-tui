# Parent full reading: 40 lifetime, leases, generations and caches
Read all 1631 lines in ranges1–410,411–820,821–1230,1231–1631. Static only, no runtime count/RSS proof.

## Integrated ownership inventory
Distinguish realm-monotonic HandleId, resource-registration generation, semantic NodeId, environment NativeRef, native runtime generation/liveness, source/source-generation/content-generation/revision, state host namespace/local slot/capture epoch. Native runtime generation initialized1, cleanup uses alive/map removal; not universal rollover epoch.
Framework wrapper/raw resource weak registry entries vs strong wrapper resource map and lease keepAlive; semantic weak-key sidecar keeps attachment wrappers strongly while node reachable. Prepared/desired/visible lease counts are not root NativeRef counts. Native slot leased Option<View> + WeakView + js_lease_count; parent graph and native component/history state may own Views independently. Cache entry, live View, leased View, mounted component, visible resource all distinct.
Temporary constructor/promotion leases drain with multiplicity, boundary root owns one, deferred desired/visible plus superseded revisions hold independently until acknowledgment. Resource disposal can fail for attachment leases; native component retirement separately defers until mount reconciliation.
State records mutable, committed Arc table demand-aware, frame overlay can pin earlier Arc despite later mutation; dirty undemanded ids remain. Content snapshots are copies, counters not RSS.
Scratch highwater capacity retained; 4096-slot page boxes free when empty but directory highwater survives, native style/path maps no normal eviction, builders/edit transactions bounded and cleared host teardown.

## Parent source-checks/corrections
Read native view_abi.rs1169–1325: release_many invokes maintain_bounded BEFORE current batch; at most256 queue candidates, threshold4096 insertions triggers full sweep. Batch release saturates zero and ignores absent slots yet reports input count; release_one_lease strict. Maintenance question §9.1 partially CLOSED: production release batches definitely run it.
IMPORTANT live scavenge candidates are discarded, NOT requeued. A zero-lease View may expire after its queue candidate was processed alive; later bounded maintenance alone need not reclaim it absent new insertions reaching threshold, lookup, or explicit full sweep. Do not claim fixed wallclock/eventual/current-live+256 bound for every idle workload; post-full-sweep bound and active-growth policy narrower. JS/native logical entries, capacities, graph bytes, allocator residency, RSS distinct.
Reports §4.4/§8.6 overstate all-install-failure rollback: direct control mutation or host desired publication can precede failing render. See parent39/31; old lease bookkeeping and visible frame preservation not native mutable-state rollback.
§4.1 resolution summary omits derivation branch; synthesis use full actual order from17/22/31. §4.2 exact hostRenderRef is lower-level/direct path, not universal public H3 deferred route.
Resource finalizeUnowned/retireRecord cleans registry metadata, does NOT call resource.dispose directly (independently read registry331–465). Native resource reclamation may then depend on wrapper/native ownership and GC, so not deterministic finalizer-triggered native disposal.
Semantic sidecar reachability is not an additional explicit-dispose veto; lease counts are veto. §8.4 no-other-semantic-reference condition TOO STRONG for explicit dispose.

## Cleanup-loop concern carried from21/34
Independently re-read runtime.ts721–843: pending length used as shrinking pass bound, finalErrors only assigned on zero-progress pass; mathematically can finish loop with pending failures and no finalErrors (e.g. one-success-per-pass chain). This report did NOT establish production dependency chain; native host content disposal/clear bindings/resource invalidateHost occurs BEFORE disposeOwnedHandles and may collapse actual dependencies. Need reachable production fixture/source argument before elevating to confirmed public shutdown defect. Do not present toy algorithm as executed Tui failure. No tests added or production edited.
