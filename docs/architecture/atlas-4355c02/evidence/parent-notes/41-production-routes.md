# Parent full reading: 41 production routes
Read all1588 lines in ranges1–400,401–800,801–1200,1201–1588. Static source route audit; no current runtime qualification.

## Canonical route matrix contribution
Public Tui direct Scene vs retained producer share deferred desired-root publication, unlike direct boundary slots/panes/history imports and benchmark hostRenderRef. Same body AND same effective History skips structural republication but validates attachments/flushes. Canonical scopes stage semantic work and History; first mount retryable after evaluation/preparation failure. ensureSemanticNative identity order semantic hint > tx-local > ceiling-gated native NodeId lookup > cycle guard > derivation > materializer. Cold/hint-miss/derivation fallback/stale retry remain retained architecture, not complete-object decode.
Text scalar cstring/UTF8 <=4 spans, packed >4; axis fixed <=4, production buffer above4; helper builder separate. Grid/diff packed. HostRenderRef resolves Rust View -> host.render (desired then synchronous frame), not production deferred root call.
Source bulk all direct content C ABI, NAPI control, feature-gated structural direct exports qualification only. Connector candidate/desired/visible association fallback preserves old selected content with error status; measurement defaults and missing paint-ticket no-op are lower-visibility exceptional paths, not newest-product fallback.
Native no-signal wait driver vs signal-aware JS polling are genuine driver alternatives, headless vs real are backend alternatives. Native History physical transfer vs semantic residence is irreversible frontier logic, not alternate structural transport.
Ordinary slot/pane direct install, native animation setup fixed/variadic frames, tick native, History push/freeze refs, state envelopes transactional validation, explicitclose aggregate cleanup. Hidden Rust binding funcs are used bridge dependencies; do not label obsolete merely hidden.

## Reconciliation/corrections
Exact renderExact helper is not called by public Tui path; independently confirmed runtime comment/callgraph in parent30, consistent42. Main warm deferred route still uses identity hint but not immediate hostRenderRef helper.
Report5.1 overstates old slot builder/root/content remains authoritative on native install failure. Parent39 source proves Rust slot set_view mutates view/frames/revision BEFORE invalidate_host may render/error. TS old bookkeeping does not rollback native component. Parent31 confirms host.render similarly advances desired before failed flush. Keep structural prep refusal, native mutation, visible frame separate; 8.6 simple two-stage summary valid for deferred Tui but not blanket across synchronous control installs.
Source seal is not universal future-mutation ban; truncate_head allowed (parent35).
No-signal wait can drive direct per-host flush without environment drain (parent34); environment scheduling table not universal.
Physical terminal failed bytes cannot be undone by logical receipt rollback; pending/candidate retention differs by failure phase. Hidden bridge Rust View path is authoritative rendering, not proven compatibility residue; use neutral term implementation bridge.
Report8.3 calls direct content lane legitimate; still compare handoff ContentDataTransport abstraction requirement and source import coupling, do not treat lane existence as owner acceptance. Source description authority != intended-contract authority.
Approx LOC estimates inconsistent with subsystem reports; no census from these values.

## Follow-up inputs
Grouped transport: exact helper nonpublic route, direct content import/oracle purpose, unsafe exports default vs feature distinctions, T15 actual route. Group lifecycle: native install may partially mutate before failure; environment wait acknowledgment vs direct driver; ticket absence/defaults as observability questions not automatically defects. These align existing parent register findings.
