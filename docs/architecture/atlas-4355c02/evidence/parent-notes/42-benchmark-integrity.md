# Parent full reading: 42 benchmark integrity
Read all975 lines in ranges1–490,491–975; source-derived audit, no benchmark rerun.

## Established critical result
Independently confirmed earlier by parent26 full current T15 script + ci.yml: T15_TRANSPORT used only output metadata. Both generated_safe_napi and feature_gated_direct_ffi CI arms import nativeViewAbiSession + RetainedRootBoundary + generated session calls. Feature stages extra exported structural symbols, does NOT select raw structural FFI. Thus direct CI qualifies export presence and NAPI execution on feature build, not two-transport performance. Historical019a048/e1fdd93 artifacts not current4355 measurements; current single-case runner lacks historical matrix/direct-arm orchestrator, cannot establish historical fraudulent route from present script alone.

## Measurement boundaries
T15 constructs NativeTuiHost + nondeferred boundary; measures semantic fixture creation + retained preparation/materialization + immediate hostRenderRef/host frame + leases. No Tui producer, shared retained execution, attachment preparation, History sideband, deferred publication or environment broker acknowledgment. Note report4.1 says no environment frame receipt, not absence of all Rust backend receipt logic: immediate host.render still Rust frame.
Production Tui direct/producer deferred boundary differs; optional route field retained notasserted by benchmark, undefined preparation throws. Internal counters establish broad identity/materializer behavior but not physical ABI.
perf13_h_content: appends encode/call direct content C ABI/decode/wake, subsequent flush measures native content/frame; initial View.content uses NAPI deferred structure. Not a pure structural or zero JS encoding metric.
Rust tui_perf: in-process vf, ResolveSession component path, LayoutCache, ViewCompiler/Painter/cache, History; binary requires perf-counters. Labels FRESH and REBUILT_EQUIVALENT same arms confirmedparent26; begin_epoch(theme default) not changing theme. Timing atomic feature differs default.
L1trace structural directboundary plus statecanonical and contentdirectScene AppHarness; textlanes headless publicTui coldmeasure. Ignoredl13_* inspected not tracked baseline, qualify supplemental artifact evidence only.
Counters: JS ordinary object, global process; Rust featuregated atomics/testthreadgate; NapiView* declared but increments absent peraudit, no physical-transport ledger. Optional Rust counters null notzero-work.

## Provenance/guardrails
T15 selfasserted transport/processisolation/revision/hash metadata is not proof, no sampler seed for random bootstrap (parent26). No current source route fallback complete-object, static absence not future gate. Historical authoritative adjective is historical label, not contemporary oracle.
Report8.5 calls direct content path intended; necessity of ContentDataTransport seam still open oracle deviation, not settled by mixed-lane design. Report8.2 older route cascade removal requires explicit supersession evidence rather than source-only authority (44 follow-up).
No benchmark corrective implementation in task. Keep measurement defect, observability gaps, production path mismatch, artifact historical limits distinct.
